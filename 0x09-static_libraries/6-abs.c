#include "main.h"
/**
 * _abs - check th code for Holberton Schol student.
 *
 * @n: is an integer argumnt
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
